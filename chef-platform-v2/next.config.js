/** @type {import('next').NextConfig} */
const nextConfig = {
  // This is a minimal configuration.
  // We can add the asset prefix or analyzer configuration later if needed,
  // but keeping it simple is best for solving the error now.
};

module.exports = nextConfig;
